apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: budget-app
  labels:
    app: postgres
spec:
  ports:
  - port: 5432
    targetPort: 5432
  selector:
    app: postgres  # Must match your StatefulSet's pod labels
  clusterIP: None

